    <footer style="text-align: center">
        {% if app.session.get('login_method')  %}
            <p>Logged using {{ app.session.get('login_method') }}</p>
        {% endif %}
    </footer>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.1/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>