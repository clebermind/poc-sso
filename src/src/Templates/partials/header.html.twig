<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Simpro | SSO POC</title>

    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.1/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>

    <nav class="navbar navbar-expand-lg navbar-light bg-light">
        <div class="collapse navbar-collapse">
            <ul class="navbar-nav mr-auto mt-2 mt-lg-0" style="margin-left: 10px">
                <li class="nav-item active">
                    <a class="nav-link" href="/dashboard/user">User</a>
                </li>
                <li class="nav-item active">
                    <a class="nav-link" href="/dashboard/admin">Admin</a>
                </li>
                <li class="nav-item active">
                    <a class="nav-link" href="/dashboard/identity-providers">List of Identity Providers</a>
                </li>
                <li class="nav-item active">
                    <a class="nav-link" href="/users">List of Users</a>
                </li>
            </ul>

            <div style="position: absolute; right: 15px;">
            {% if is_granted("ROLE_USER") %}
                <a class="nav-link" href="/logout">Logout</a>
            {% else %}
                <a class="nav-link" href="/login">Login</a>
            {% endif %}
            </div>
        </div>
    </nav>